# 依赖 —— 按本机 pip freeze 的实际结果写死版本
# 生成方式： .\.venv\Scripts\python -m pip freeze
#
# 说明：文档里建议 Prometheus 3.14.0 / Grafana 13.2.1 / prometheus-client 0.22.1。
# Prometheus 与 Grafana 是**独立程序**，不是 pip 包，不在这份清单里。
# prometheus-client 实际装到的是 0.26.0（Python 3.13 环境下没有 0.22.1 的可用组合）。

# ---- 被监控应用 ----
fastapi==0.141.1
uvicorn==0.53.0
prometheus-client==0.26.0
psutil==7.2.2              # 原生 Windows 补进程 CPU/内存指标（标准库拿不到）

# ---- 仅测试用 ----
httpx==0.28.1              # FastAPI TestClient 需要
pytest==9.1.1
pyyaml==6.0.3              # 校验 prometheus.yml / alert-rules.yml 结构

# ---- 传递依赖 ----
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
colorama==0.4.6
h11==0.16.0
httpcore==1.0.9
idna==3.19
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pydantic==2.13.5
pydantic-core==2.46.5
Pygments==2.21.0
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0

# ---- 需要单独下载安装的（不是 pip 包）----
# Prometheus 3.x  windows-amd64.zip   ~100MB
#   https://github.com/prometheus/prometheus/releases
# Grafana OSS 13.x  windows-amd64.zip ~200MB
#   https://grafana.com/grafana/download
#
# 本机状态见 docs/ceshi-jilu.md —— 下载未完成，所以 Prometheus/Grafana 未实跑。
